# ---------------------------------------------------------------------------
# OPTIONAL deep-learning / object-detection extras.
#
# These are NOT required to run the platform. With DEMO_MODE=true (and even
# with DEMO_MODE=false) the platform falls back to the OpenCV + scikit-learn
# baselines that ship in `backend/app/ml/`.
#
# Install only when you intend to TRAIN or SERVE real neural networks:
#     pip install -r requirements-ml.txt
#
# NOTE: torch wheels are platform/CUDA specific. For a CPU-only install see
#       https://pytorch.org/get-started/locally/
# ---------------------------------------------------------------------------

torch>=2.5,<3.0
torchvision>=0.20,<1.0

# Keras/TensorFlow CNN freshness classifier (see backend/app/ml/freshness/cnn_model.py).
# Required to SERVE a .keras artefact such as freshness_cnn.keras.
tensorflow>=2.20,<2.22

# YOLO-based spoilage object detection (see backend/app/ml/spoilage/yolo.py).
# Ultralytics is AGPL-3.0 licensed - review the licence before redistributing.
ultralytics>=8.3,<9.0

# Gradient boosting alternative for the shelf-life regressor.
xgboost>=2.1,<3.0

# Training-time helpers
matplotlib>=3.9
seaborn>=0.13
tqdm>=4.66
albumentations>=1.4
